# Development and test dependencies (mirrors pyproject.toml [project.optional-dependencies] dev)
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-qt>=4.4.0
hypothesis>=6.100.0          # property-based tests (tests/test_config_v2.py)

# Fast hashing used by the engine (optional at runtime, required to test it)
xxhash>=3.4.0

# Linting / typing / formatting / security
black>=24.0.0
flake8>=7.0.0
mypy>=1.10.0
ruff>=0.4.0
pre-commit>=3.7.0