# Core dependencies
click>=8.0.0
psutil>=5.9.0
pyyaml>=6.0.0

# Trash support (used by deleter.py)
send2trash>=1.8.0

# Optional GUI dependencies
# PySide6>=6.5.0

# Optional dependencies for enhanced features
# plotly>=5.15.0       # For visualization features
# docker>=6.0.0       # For Docker cleanup features
# pyarrow>=12.0.0     # For Parquet exports (cotrex_tree Apex Edition)
# requests>=2.28.0    # For network operations

# Platform-specific dependencies
# Windows
pywin32>=306; sys_platform == "win32"

# macOS
# pyobjc>=9.0; sys_platform == "darwin"

# Linux
# python-xlib>=0.33; sys_platform == "linux"