# Core dependencies
click>=8.0.0
psutil>=5.9.0
pyyaml>=6.0.0
babel>=2.12.0
keyring>=24.0.0

# Optional GUI dependencies (deep-cleaner[gui])
PySide6>=6.5.0

# Optional dependencies for enhanced features
plotly>=5.15.0  # For visualization features
docker>=6.0.0   # For Docker cleanup features
requests>=2.28.0  # For network operations

# Platform-specific dependencies
# Windows
pywin32>=306; sys_platform == "win32"

# macOS
pyobjc>=9.0; sys_platform == "darwin"

# Linux
python-xlib>=0.33; sys_platform == "linux"